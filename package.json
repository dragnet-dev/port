{
    "name": "port",
    "scripts": {
        "build": "tsc && esbuild src/index.ts --bundle --outfile=public/_worker.js --format=esm --platform=browser && esbuild src/hljs-bundle.js --bundle --outfile=public/assets/highlight.min.js --format=iife --minify && node scripts/emit-styles.mjs && node scripts/emit-sitemap.mjs",
        "dev": "wrangler pages dev public --kv CACHE",
        "test": "vitest run --passWithNoTests",
        "typecheck": "tsc --noEmit"
    },
    "dependencies": {
        "hono": "^4.0.0",
        "js-yaml": "^4.1.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.0.0",
        "@types/js-yaml": "^4.0.9",
        "esbuild": "^0.28.0",
        "highlight.js": "^11.0.0",
        "tailwindcss": "^4.3.0",
        "typescript": "^6.0.3",
        "vitest": "^4.1.7",
        "wrangler": "^4.94.0"
    }
}
